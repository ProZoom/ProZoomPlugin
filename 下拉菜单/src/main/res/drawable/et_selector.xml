<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
    state_focused:
    state_window_focused:
    state_pressed 是否按下，如一个按钮触摸或者点击。
    state_focused 是否取得焦点，比如用户选择了一个文本框。
    state_hovered 光标是否悬停，通常与focused state相同，它是4.0的新特性
    state_selected 被选中，它与focus state并不完全一样，如一个list view 被选中的时候，它里面的各个子组件可能通过方向键，被选中了。
    state_checkable 组件是否能被check。如：RadioButton是可以被check的。
    state_checked 被checked了，如：一个RadioButton可以被check了。
    state_enabled 能够接受触摸或者点击事件
    state_activated 被激活
    state_window_focused 应用程序是否在前台，当有通知栏被拉下来或者一个对话框弹出的时候应用程序就不在前台了
    -->
    <item android:drawable="@drawable/et_foucs"
          android:state_focused="true"
          android:state_window_focused="true"/>

    <item android:drawable="@drawable/et_normal"/>
</selector>